#! /usr/bin/bash

set -e # stop if it fails at any point
CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="bootstrap"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"


ABS_PATH=$(readlink -f "$0")
BIN_PATH=$(dirname $ABS_PATH)



source "$BIN_PATH/backend/generate-env"
source "$BIN_PATH/frontend/generate-env"